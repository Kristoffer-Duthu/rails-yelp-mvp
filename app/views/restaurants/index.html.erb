<h1> YELP! I NEED SOMEBODY YELP!!! NOT JUST ANYBODY </h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>

<%= link_to "Add Restaurant", new_restaurant_path(@restaurant) %>
